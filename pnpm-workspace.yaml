# ai-plugin/ builds the generator and ships its Claude Code plugin; packages/ is
# what the design system ships. The npm scopes say the same thing —
# @figma-to-react/* against @ds/* — so "package" keeps its distribution meaning.
packages:
  - 'ai-plugin/*'
  - 'packages/*'

onlyBuiltDependencies:
  - esbuild
