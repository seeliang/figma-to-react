// Moved to @figma-to-react/testing so every layer package can share one implementation.
// Kept here because generated stories import it by this path; the generator
// will point at the package directly once the layers are split out.
export * from '@figma-to-react/testing/theme'
