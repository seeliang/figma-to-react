{
  "name": "figma-to-react-workspace",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter './tools/*' --filter './packages/*' run build",
    "test": "vitest run",
    "typecheck": "tsc -b tools/core tools/emit-react tools/emit-storybook tools/cli tools/config tools/testing",
    "format": "prettier --write .",
    "ds:init": "figma2react init",
    "ds:gen": "figma2react gen",
    "ds:theme": "figma2react theme",
    "ds:tokens": "figma2react tokens",
    "ds:audit": "figma2react audit",
    "verify": "pnpm build && pnpm typecheck && pnpm test && node scripts/verify-skills.mjs && pnpm --filter figma-to-react-example build && node scripts/verify-styles.mjs && node scripts/verify-tokens.mjs && pnpm --filter figma-to-react-example build-storybook && node scripts/verify-styles.mjs --dist examples/storybook-static/assets && pnpm --filter figma-to-react-example test-storybook"
  },
  "devDependencies": {
    "@figma-to-react/cli": "workspace:*",
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
